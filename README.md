# LangChain & Chroma RAG Assistant 🤖
This is a Retrieval-Augmented Generation (RAG) pipeline built with Python, LangChain, ChromaDB, and Streamlit.

## How to run
1. Install requirements: `pip install -r requirements.txt`
2. Run the app: `streamlit run src/app.py`